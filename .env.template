# Google OAuth Environment Variables
# Replace these values with your own Google Cloud credentials
# Do NOT commit the actual .env file with real credentials to version control

# OAuth Client ID from Google Cloud Console
GOOGLE_OAUTH_CLIENT_ID=your_client_id_here

# OAuth Client Secret from Google Cloud Console
GOOGLE_OAUTH_CLIENT_SECRET=your_client_secret_here

# Database URL - PostgreSQL connection string (if using PostgreSQL)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/calendar_extractor

# Flask session secret key - used for session encryption
# Generate a strong random key, e.g., using: python -c "import secrets; print(secrets.token_hex(16))"
SESSION_SECRET=generate_random_secret_key_here

# Replit domain - this is automatically set in Replit environment
# Only needed when running locally for development
# REPLIT_DEV_DOMAIN=your-repl-name.username.repl.co

# Instructions:
# 1. Copy this file to .env (e.g., cp .env.template .env)
# 2. Replace the placeholder values with your actual credentials
# 3. Make sure .env is in your .gitignore to prevent accidental exposure of secrets
# 4. In Replit, use the Secrets tool instead of this file